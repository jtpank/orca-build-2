//TODO:
// Setup isolated caching functionality to reduce code bloat
export default function setCache(key, value) {
    //store cache
}
export default function getCache(key, value) {
    //store cache
}
export default function clearCache (key, value) {
    //store cache
}
